#include <stdio.h>

int main() {
   // printf() displays the string inside quotation
    int x = 0;
    for (int i = 0; i < 10; i++) {
      x += i;
    }
    printf("Hello, World! Num: %d\n", x);
    return 0;
}

